#ifndef FILTER_DEF_H_
#define FILTER_DEF_H_

// MASK_SIZE is used to specify the depth of the mask.
const int MASK_N = 1;

// MASK_WIDTH and MASK_HEIGHT define the dimensions of the Gaussian mask.
const int MASK_X = 5;
const int MASK_Y = 5;

#endif // FILTER_DEF_H_
